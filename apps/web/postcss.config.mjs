// Tailwind v4 is a PostCSS plugin and needs no tailwind.config.js - the theme lives in
// app/globals.css, matching the shape q.css already had.
export default {
  plugins: { '@tailwindcss/postcss': {} },
}
