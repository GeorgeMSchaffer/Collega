{
  "name": "collega-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright browser E2E tests for Collega (drives the Blazor client at :5098 against the API at :5103).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.2"
  }
}
