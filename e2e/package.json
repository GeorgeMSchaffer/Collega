{
  "name": "collega-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright browser E2E tests for Collega (drives apps/web at :3000, which Playwright starts itself).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
