{
  "name": "collega",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Root tooling for the JavaScript/TypeScript satellites around the .NET application: the e2e Playwright suite, the golden-file harness, and the Tailwind build for the comps. The product itself is .NET 8 / ASP.NET Core / Blazor WebAssembly - see Collega.sln.",
  "packageManager": "pnpm@12.3.4",
  "engines": {
    "node": ">=24.20.0",
    "pnpm": ">=12.3.4"
  },
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "check": "turbo run lint typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "@types/node": "26.4.1",
    "turbo": "2.10.12",
    "typescript": "7.0.2"
  }
}
