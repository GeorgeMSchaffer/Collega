# Bug Triage

This document is the authoritative queue for bugs and minor tweaks that must be addressed before new feature work begins.

## Workflow

- Read this document before starting or resuming feature implementation.
- Items under `TODO` take priority over new features. Do not start a new feature while `TODO` contains unresolved items unless the user explicitly approves an exception.
- When an item is fixed and its focused validation passes, remove it from `TODO` and add it to `COMPLETED` with the completion date and a concise verification note.
- Do not duplicate an item between sections. If a change is incomplete, unverified, or deferred, keep it under `TODO` and note its status there.
- New bugs and minor tweaks belong under `TODO`; feature ideas remain in the delivery backlog.

## TODO
* My profile should allow for the uploading of a portrait, if the portrait is set it should be display instead of the current avatar circle with initals where every the. The size of both the portrait should be 25 px high and wide. Also add validation and security checks so the user cannot uplad a malicous content instead of a GIF, Jpeg, and PNG. The uploaded image should be resized to fit the 25px maximum height / width rule. *(Scheduled as Sprint 4 P1; image library approved = SkiaSharp.)*
* **[Idea Brainstorm chat — user decisions recorded 2026-08-11]** The ChatGPT-style brainstorm chat that now fronts idea creation on the Ideas list (`Components/IdeaBrainstormModal.razor`, wired into `Pages/Ideas.razor`; seeds `IdeaCreateModal.InitialDescription`) shipped as a first pass. The three parked questions are now resolved:
    1. **Add New button visibility** — DECISION: **give Site Admins a create path.** Add an org picker step before the board picker so a Site Admin (who has no single org/board context) can create an idea into a chosen org/board. Currently gated OFF via `Pages/Ideas.razor` `@if (!_isReadOnly && !_isSiteAdmin)`; Read-Only stays gated off. This is a new UI task — build it **comp-first** (see item below). Tracked as its own TODO entry below.
    2. **AI vs. plain chat** — DECISION: **keep it plain for now, revisit a real LLM-backed conversation after the current sprints (post-Sprint-4).** No NuGet/API-key work now. Component stays structured to swap `_NextAssistantPrompt` for a model call later. This is a deferred, post-MVP-sprint item — do not start it inside Sprints 1–4.
    3. **Process** — DECISION: **comp-first going forward.** New page/flow UI gets a throwaway HTML comp in `SPEC/mockups/` for sign-off before production Blazor, per `CLAUDE.md`'s worktree workflow. (Small/obvious tweaks to already-locked surfaces don't need a comp.)
* **[Added 2026-08-11]** Site Admin idea-create path: give Site Admins an "Add New" idea flow with an **org picker step before the board picker** (Read-Only stays gated off). Build **comp-first** — throwaway HTML in `SPEC/mockups/` for sign-off before Blazor. (Resolution of Idea-Brainstorm open question #1 above.)
* **[Discovered 2026-08-10, during Sprint 1 verification]** Applying the idea-type / business-impact migration to a database that already contains idea rows fails on `FK_ideas_business_impacts_business_impact_id` — existing `ideas.business_impact_id` values are not all present in `business_impacts`. A fresh/empty database migrates cleanly; a populated one does not. The migration likely needs to create and backfill `business_impacts` before adding the FK. Needs confirmation and a fix before any deployment applies migrations over existing data.

* The Detail forms should be a drawer pattern, not a modal
* Create an Org and a Site Admin user in the auth seed data so we can test it from the site admin perspective (and not just the org admin perspective) — this is a pre-MVP affordance, not a post-MVP one.
* We should add the ability to view the app as another user.  For example a user dropdown for "View As" that allows the site admin to view the app as any user in the system. Site Admin can do this for all users and site admins can do so for the active users in their org. We can probally place this centered vertically in the top nav bar, aligned to the right.
* Manage button on the Idea List Items does not seem to do anything. It should use the same drawer pattern as the other detail forms, not a modal.
* Manage button on "Idea Types" list items does not seem to do anything. It should use the same drawer pattern as the other detail forms, not a modal.
* The Idea board should use the same drawer pattern as the other detail forms, not a modal.


## IDEAS

Help me come up with a new feature.  The overall idea is that the board is used to manage the process of brainstorming and fleshing out ideas, think of it as a Trello, Jira, Workspace hybrid.  I want to enable the user to manage the process of implementing the idea using AGILE Best practices.  Admins should be able to create Roadmaps. These roadmaps, will consit of Sprints, which consits of Tasks, which are Ideas translated to tasks.  However the idea seems a bit uncooked and potential akward.  Help me to refine the idea for this feature by making recommendations, referencing best practices, and ideas that could differantiate product from Trello and Jira.

    * Main Funtionality
        * Create Roadmap(s)
            * Should have the following fields
                * Title
                * Goal Description Field
                * Start Date
                * End Date
                * The ability to assign Roadmap Owner(s)

        * Sprints should be assignable to a Road Map, the fields on the Sprint:
            * Title
            * Start Date
            * End Date
            * Sprint Goal
            * Sprint Owner
            * Tags
        * Sprints will consits of one to many Issues.  Issues are an extension of an Idea, however it should have additional fields for:
            * Start Date
            * End Date
            * Effort (Low, Medium, High)
            * Sprint Tags
            * Status: Pending, Scoping, Development, Review and Complete


## COMPLETED
- 2026-08-11 — **All-list-pages filters — delivered** (bookkeeping reconciliation). "Search field searching each displayed column" is satisfied on every admin list by the shared `ListToolbar` all-column search (Sprint 2 / T-UI-2) and on the global Ideas list by Sprint 3's server-side all-column search. The "tag search" and "user-association (created-or-assigned) filter" sub-requirements apply only to the tag-/user-bearing entity — Ideas — where Sprint 3 added a `tag` filter and a `user` (authored-or-assigned) filter to `GET .../ideas` and matching client controls. Admin entities (Organizations/Users/Statuses/Idea Types/Custom Fields) carry no tags/assignees, so those two sub-filters don't apply there. Verified via Sprint 3 (`SPEC/sprints/archive/sprint-03-list-filter-parity.md`, SQL-translatability live-verified). *If you intended tag/user filters on some entity beyond Ideas, reopen with that entity named.*
- 2026-08-11 — **Ideas-list sorting — delivered** (bookkeeping reconciliation; the original TODO line "On the Ideas list view we should allow for sorting by" was an unfinished sentence). Sprint 3 added server-side column sort on Title, Created By, Assigned To (alphabetically-first assignee), Status, and Created Date — ascending/descending with a stable idea-id tiebreaker — plus sortable client headers on `/ideas`. Verified via Sprint 3 (archived plan above).
- 2026-08-11 — **Admin list/detail drawer layout — delivered** (bookkeeping reconciliation). Sprint 2 converted Organizations, Users, Statuses, Idea Types, and Custom Fields to the List + Drawer/Modal pattern, consistent with the Ideas list/detail, retiring the prior full-page/inline-edit surfaces (shared `DrawerShell`/`CreateModalShell`). Verified via Sprint 2 (`SPEC/sprints/archive/sprint-02-drawer-pattern-rollout.md`).
- 2026-08-10 — **Sprint 1 (Bug Triage quick wins) completed** (client-only; branch `feature/sprint-01-bug-triage-quick-wins`). (1) Uniform **"Add New"** label on every list page's primary create action (Organizations, Boards, Statuses, Idea Types, Custom Fields, Users, Ideas), keeping the existing right-alignment; (2) the **Back** action moved to its own left-aligned row above the command bar on all six admin list pages (new `.backrow` CSS) — it uses `history.back`, so it returns to the page the user came from; (3) Custom Fields **"Show archived"** checkbox moved to immediately right of the search box; (4) removed the Site-Admin create action that dead-ended to the organizations list on the global Custom Fields / Statuses / Idea Types / Users lists; (5) Home **"Platform administration"** section removed (All boards + activity feed remain); (6) `/settings` link labels **title-cased** (My Profile, Manage Users, Manage Boards, Manage Statuses, Manage Idea Types, Manage User-Defined Fields); (7) Ideas edit-mode inputs (full-width / 50-50) confirmed **already satisfied** by the T-UI-3 drawer (`.formrow`/`.field` CSS) — no change needed. **Verified:** clean client build (0 errors; the pre-existing `BoardDetail.razor:456` nullable warning is unrelated); full solution suite **474 green**; and a live run (API :5103 + client :5098 against a freshly reset + demo-seeded SQL) confirming, as demo Org Admin, the title-cased `/settings` and the Back→Search→(Show archived)→Add New ordering on the Custom Fields and Statuses lists, plus the Org-Admin Home (boards + activity feed). The two Site-Admin-only changes (Home Platform-admin removal, global create-dead-end removal) are conditional-branch deletions verified by code + build rather than a live Site-Admin session (to avoid exposing the seeded Site-Admin secret). Bookkeeping note: the prior 2026-08-10 "Add-new button right-alignment" entry below covered right-alignment only; this entry closes the label-uniformity and back-button/archived-checkbox placement parts that were still open.
- 2026-08-10 — **T-UI-3 Idea Detail slide-in drawer + create modal completed** (merged to `dev`). Full-page `IdeaDetail.razor` (`/ideas/{id}/edit`) retired; `?idea={id}` + bare `/ideas/{id}` open the Ideas list with the drawer; board uses `/board/{id}?idea={id}`. `Components/IdeaDrawer.razor` (full parity: fields + UDFs, tags, mentions, 0–5 assignees, comments, upvote, status move with live re-slot, admin delete + reassign) and `Components/IdeaCreateModal.razor`, wired into the Ideas list and board rows/cards. **Verified:** clean client + full-solution builds; full **474-test** suite green; independent code-review pass (5 findings fixed — cross-idea comment-draft leak, interleaved-load guard, Site-Admin create dead-end, nullable `OnCreated`, upvote aria-label); and a **live run** (API :5103 + client :5098 + demo-seeded SQL) confirming, as demo Org Admin, the Ideas list with the new Type/Status filters + rows-per-page pager, the drawer opening with full-parity content via the bare `/ideas/{id}` route, and the board swim lanes rendering with the drawer overlaid via `/board/{id}?idea={id}`. (Read/render/routing exercised live; mutation round-trips — save/comment/upvote/status-move/create-submit — are covered by the 474 tests + code-review contract conformance + the user's own confirmation that the Ideas drawer "works great." The headless browser pane could not reliably drive Fluent form clicks, so live mutation clicks were not scripted.)
- 2026-08-10 — **T-UI-2 List View enhancements completed** (part of the T-UI-3 pass): a shared `Components/ListToolbar.razor` + `ListState.cs` add all-column search + a 10/25/50/100 page-size selector + prev/next pagination to all six admin lists (Organizations, Organization Users, Boards, Statuses, Custom Fields, Idea Types); the Ideas global list gained Idea Type + Idea Status dropdown filters alongside its existing chips/search/paging. Manual reorder on Statuses/Idea Types/Custom Fields is intentionally gated to the unfiltered single-page view (index-based persistence). Verified by clean client + full-solution builds and the 474-test suite; covered in the code-review pass (search projections cover every displayed column; page index self-heals).
- 2026-08-10 — **T-UI-1 Navigation icon family completed:** the rail was already fully on Fluent System Icons; the residual non-navigation chrome glyphs were converted for consistency — BackButton `←`→ArrowLeft, upvote `▲`→CaretUp (drawer + board), comment `💬`→Comment (board), admin reorder handles `⠿`→ReOrderDotsVertical (Statuses/Idea Types). The Idea Type badge emoji picker was correctly left as intentional product data; the `→` in `IdeaFieldInputs` was confirmed to be comment prose, not a UI glyph. Verified by clean client + full-solution builds.
- 2026-08-10 — Add-new button right-alignment on list pages completed: the primary "New …" action now sits on the right of the `.cmdbar` header (via `margin-left:auto`) on Settings→Organizations, Boards admin, Statuses admin, Custom Fields admin, Idea Types admin, and Organization Users (both active and Site-Admin fallback variants; on Organization Users the whole action cluster right-aligns with Back kept on the left). Verified by a clean `Collega.Client` build (0 errors; the single pre-existing BoardDetail.razor:456 nullable warning is unrelated).
- 2026-08-09 — Site Admin global visibility and stale-session handling completed: Site Admin Home, Boards, Ideas, Users, Statuses, and User-Defined Fields aggregate every active organization with explicit organization labels and scoped management links; Board and Idea detail derive related-data context from the owning board rather than an account membership. Every authenticated API path now clears an invalid persisted token and redirects to Login with the expired-session message, and Development HTTP hosting no longer runs HTTPS redirection. Verified by two clean Release solution builds, two full 425-test passes, and a live browser reproduction that cleared the stale token and reached `/login?sessionExpired=true` instead of leaving the page on 401.
- 2026-08-09 — Settings information architecture completed: `/settings` is a role-scoped link-card hub; organizations moved to `/settings/organizations`; Org Admin users moved to `/settings/users`; and Users, Statuses, and User-Defined Fields link to dedicated list/form views. Site Admin organization rows expose scoped Users, Statuses, and Fields actions. Verified by focused and full solution builds plus live Org Admin/member browser checks, including invite-code preservation, authorization, and a 375px responsive pass.
- 2026-08-09 — Session lifetime completed: verified the 480-minute absolute expiry in persisted browser state, the warning dialog and countdown after 28 minutes idle, activity reset through Stay signed in, automatic logout after 30 minutes idle, expired-session confirmation, and logout synchronization across two browser tabs.
- 2026-08-09 — Password-change redirect completed: changed a demo Org Admin password through Profile, verified authentication was cleared and the user was redirected to Login with confirmation, signed in with the new password, and restored the original demo password through the same flow.
- 2026-08-09 — Text input vertical centering completed: verified Profile's Fluent fields render at 36px with 34px inner inputs and zero vertical input padding; at 375px all fields fit without horizontal page overflow.
- 2026-08-09 — Fluent icon refresh completed: verified Fluent System Icons render in the navigation rail and board drag handles with accessible names, stable 20px rail dimensions, and responsive desktop/mobile account-menu behavior.
- 2026-08-09 — Development seeder completed: exactly 2 organizations, each with one Org Admin, two Users, two boards, and 11 ideas per board distributed `3/2/2/1/3`; one global Site Admin remains organization-independent. Verified by 10 focused seeder tests, full 417-test solution pass, and a fresh LocalDB `Collega` run showing 2 organizations, 6 organization users, 1 global Site Admin, 4 boards, 44 ideas, and zero cross-organization ownership violations.
