# Collega Implementation Agent Tracker

## Purpose
Track what's true right now: current implementation status and what's next. **This file is kept short and current-only on purpose** — full narrative history (per-slice build write-ups, judgment calls, UI comp sign-off history, the original T001-T052 backlog) lives in `SPEC/implementation-agent-tracker-archive.md`. Read this file for "what's true right now"; read the archive for "how did we get here" or "what did slice X actually build."

## Ground-Truth Verification — read this before trusting anything below
Before making any status, planning, or scope claim about this project — in this session or any future one — re-read the Current Status section below AND run `git log --oneline -10` fresh in that same turn. Never answer from recollection, even within the same conversation. This file was split from a 291-line narrative log on 2026-08-10 specifically because an agent answered a planning question from stale in-context memory while ~2 weeks of real parallel-agent work had landed without that memory being refreshed. A large date jump, an unfamiliar recent commit, or "it's been a while since I checked" are signals to verify more, not less. See also `CLAUDE.md`'s "Ground-Truth Verification" section.

## Pre-Feature Triage Gate
- Before starting or resuming implementation, read `SPEC/Bug Triage.md`.
- Unresolved items in its `TODO` section take priority over new feature work. Do not start a new feature until those items are cleared unless the user explicitly approves an exception.
- After a fix is complete and focused validation passes, move the item from `TODO` to `COMPLETED` with its completion date and verification note; do not retain it in both sections.

## Current Status
- **As of 2026-08-10: all originally-scoped MVP epics are merged into `dev`.** Foundation through Hardening (T001-T052), User-Defined Fields (T053-T060, pulled into MVP scope 2026-08-08), and Idea-Type Fields (T061-T067) are all built and merged. Full Blazor client exists: 18 pages, 7 shared components (`IdeaDrawer`, `IdeaCreateModal`, `ListToolbar`, `IdeaFieldInputs`, `BackButton`, `SessionTimeoutGuard`, `TypeBadge`).
- **Test suite last reported: 474 green** (as of the T-UI-3 merge, 2026-08-10). Re-verify with `dotnet test Collega.sln` before trusting this number if it's been a while.
- **Idea Detail surface**: a right slide-in drawer + centered create modal (`IdeaDrawer.razor`/`IdeaCreateModal.razor`), reached via `?idea={id}` over the Ideas list or a board, or bare `/ideas/{id}`. This superseded an earlier full-page `/ideas/{id}/edit` design — that route is retired.
- **Remaining pre-MVP work is scoped in `SPEC/95-next-sprints.md`**: `SPEC/Bug Triage.md`'s open `TODO` list, a drawer-pattern rollout to Organizations/Users/Statuses/Idea Types/Custom Fields (Sprint 2, which also carries an adopted opt-in Site Admin seed-reset flag — `--seed:auth=reset`), list filter parity + server-side sort (Sprint 3), a deferred QA/Code-Review pass, and profile portrait upload. **Start there for what's next** — this file's archive has the original T001-T052 task breakdown, but that's all done; don't restart it.
- **Sprint 1 (Bug Triage quick wins) is COMPLETE and merged to `dev` (2026-08-10)** — uniform "Add New" labels, back-button-above-search + `.backrow`, Custom Fields archived-checkbox placement, Site-Admin create dead-end removal, Home Platform-admin section removal, `/settings` title-casing. Archived at `SPEC/sprints/archive/sprint-01-bug-triage-quick-wins.md`. **Next sprint is Sprint 2 (drawer-pattern rollout).** A migration bug was discovered and filed to `Bug Triage.md` TODO during Sprint 1 verification: the idea-type/business-impact migration fails its FK when applied over a DB that already has idea rows.
- **QA/Code Review has been skipped on nearly every merge so far**, by standing user direction, to move fast through the original MVP build-out. That's real accumulated debt — addressed in Sprint 4 of `95-next-sprints.md`, not before.
- Known judgment calls awaiting human review (full list + context in the archive): lockout is a fixed-window approximation not a true sliding window; JWT signing key is ephemeral per-process unless `Auth:TokenSigningKey` is configured (must be set before any multi-instance/long-uptime deployment); `Status` name is modeled as `nvarchar(100)` though an early comp hinted at 25 (would need a migration to change); default `Status.Color`/`SortOrder` values are implemented but not yet human-confirmed as final.
- Test data note (updated 2026-08-10): the local `Collega` database on the `collega-sqlserver` Docker container (host port **1434**; the API's user-secrets connection string points here) was **dropped and re-seeded** during Sprint 1 live verification (user-approved), so the previously-noted manual records ("Quinn Tester" / "QA Test Org" / "QA Board") are gone — it now holds only the standard demo seed (2 orgs, 6 org users + 1 Site Admin, 4 boards, ideas). Note also there is an unrelated `sql-server-wwi` container occupying host port 1433 that can shadow LocalDB/1433 connection attempts.
- Last updated: 2026-08-10 (Sprint 1 completion — update this line whenever this section changes, not just when something is appended).

## Notes For Next Agent
- Read `SPEC/95-next-sprints.md` for current sprint scope, not the archive's original backlog.
- Use `Specs Overview.md` as the behavior entrypoint and `30-Contracts.md` for endpoint and payload authority.
- Keep this Current Status section short and current — if you're tempted to write more than a few sentences about something that happened, put the detail in `SPEC/implementation-agent-tracker-archive.md` and leave a one-line pointer here.
