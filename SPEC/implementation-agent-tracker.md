# Collega Implementation Agent Tracker

## Purpose
Track what's true right now: current implementation status and what's next. **This file is kept short and current-only on purpose** — full narrative history (per-slice build write-ups, judgment calls, UI comp sign-off history, the original T001-T052 backlog) lives in `SPEC/implementation-agent-tracker-archive.md`. Read this file for "what's true right now"; read the archive for "how did we get here" or "what did slice X actually build."

## Ground-Truth Verification — read this before trusting anything below
Before making any status, planning, or scope claim about this project — in this session or any future one — re-read the Current Status section below AND run `git log --oneline -10` fresh in that same turn. Never answer from recollection, even within the same conversation. This file was split from a 291-line narrative log on 2026-08-10 specifically because an agent answered a planning question from stale in-context memory while ~2 weeks of real parallel-agent work had landed without that memory being refreshed. A large date jump, an unfamiliar recent commit, or "it's been a while since I checked" are signals to verify more, not less. See also `CLAUDE.md`'s "Ground-Truth Verification" section.

## Pre-Feature Triage Gate
- Before starting or resuming implementation, read `SPEC/Bug Triage.md`.
- Unresolved items in its `TODO` section take priority over new feature work. Do not start a new feature until those items are cleared unless the user explicitly approves an exception.
- After a fix is complete and focused validation passes, move the item from `TODO` to `COMPLETED` with its completion date and verification note; do not retain it in both sections.

## Current Status
- **As of 2026-08-10: all originally-scoped MVP epics are merged into `dev`.** Foundation through Hardening (T001-T052), User-Defined Fields (T053-T060, pulled into MVP scope 2026-08-08), and Idea-Type Fields (T061-T067) are all built and merged. Full Blazor client exists: ~16 pages (Sprint 2 retired the full-page `OrganizationEdit`/`UserEdit`), 9 shared components (`DrawerShell`, `CreateModalShell`, `IdeaDrawer`, `IdeaCreateModal`, `ListToolbar`, `IdeaFieldInputs`, `BackButton`, `SessionTimeoutGuard`, `TypeBadge`).
- **Test suite last reported: 525 green** (2026-08-11, `dotnet test Collega.sln`: 113 Domain + 189 Application + 90 Infrastructure + 133 API). The previously-recorded 500 predated the members-endpoint merge (`d3ef5e4`/`fe2f37f`), which added 6; Sprint 4's two security items added the other 19. Re-verify before trusting this number if it's been a while.
- **Bug-triage fixes landed 2026-08-11 (pre-Sprint-4):** (1) the idea-type/business-impact migration FK failure over a populated DB is fixed (backfill added to `20260808233009_AddIdeaClassification`); (2) Site-Admin "Manage" links on the global Idea Types/Statuses/Fields admin lists now actually navigate (added `OnParametersSetAsync` reload — they'd reused the same component instance); (3) a Development-only demo Site Admin (`siteadmin@demo.collega.test` / `Abc123!`, no forced change) so the platform-admin view is testable. Details + the still-open items (portrait upload, Site-Admin idea-create path, View-As feature, and a drawer/modal cluster that looks like stale-build artifacts pending re-verify) are in `SPEC/Bug Triage.md`.
- **Idea Detail surface**: a right slide-in drawer + centered create modal (`IdeaDrawer.razor`/`IdeaCreateModal.razor`), reached via `?idea={id}` over the Ideas list or a board, or bare `/ideas/{id}`. This superseded an earlier full-page `/ideas/{id}/edit` design — that route is retired.
- **Remaining pre-MVP work is scoped in `SPEC/95-next-sprints.md`**: `SPEC/Bug Triage.md`'s open `TODO` list, a drawer-pattern rollout to Organizations/Users/Statuses/Idea Types/Custom Fields (Sprint 2, which also carries an adopted opt-in Site Admin seed-reset flag — `--seed:auth=reset`), list filter parity + server-side sort (Sprint 3), a deferred QA/Code-Review pass, and profile portrait upload. **Start there for what's next** — this file's archive has the original T001-T052 task breakdown, but that's all done; don't restart it.
- **Sprints 1, 2, and 3 are COMPLETE and merged to `dev` (2026-08-10 – 2026-08-11).**
  - **Sprint 1 (Bug Triage quick wins):** uniform "Add New" labels, back-button-above-search + `.backrow`, Custom Fields archived-checkbox placement, Site-Admin create dead-end removal, Home Platform-admin section removal, `/settings` title-casing. Archived at `SPEC/sprints/archive/sprint-01-bug-triage-quick-wins.md`.
  - **Sprint 2 (drawer-pattern rollout):** shared `DrawerShell`/`CreateModalShell` extracted from the Ideas surface; Organizations, Users, Statuses, Idea Types, and Custom Fields all converted to List + Drawer (full-page/inline-edit surfaces retired; Organizations drawer addressable at `/settings/organizations/{id}`); plus the `--seed:auth=reset` Site Admin seed-reset flag. Specs updated (`20-feature-client-ui.md` "Admin entities…" section; `-revisions.md` Decision D5). Archived at `SPEC/sprints/archive/sprint-02-drawer-pattern-rollout.md`.
  - **Sprint 3 (list filter parity + server-side sort):** the global org ideas endpoint gained all-column `search`, a `tag` filter, a `user` (authored-or-assigned) filter, and five-key server-side `sortBy` with an id tiebreaker; the `/ideas` client list got sortable headers + tag/user filter controls (compose with existing scope/type-status/field filters, page-1 reset). SQL-translatability live-verified against real SQL Server (InMemory can't prove it). Contract + `20-feature-ideas-and-engagement.md` updated. Archived at `SPEC/sprints/archive/sprint-03-list-filter-parity.md`.
  - **Sprint 4 is IN PROGRESS (started 2026-08-11).** Its two security-first items are **done** on `feature/sprint-04-security-hardening`: (1) CSV export formula injection (CWE-1236) — `Csv.Escape` guards cells starting `= + - @ \t \r`, `Csv.Parse` strips the guard symmetrically so export→re-import stays lossless; (2) the mandatory password rotation is now enforced **server-side** by a global opt-in-allowlist filter (`PasswordChangeRequiredFilter`), not just by the client's claim — plus the aggravator fix, where an unrotated temporary password no longer loses its expiry on login. Specs updated first-class (`20-feature-auth.md` #32a/#32b, `30-Contracts.md` → "Mandatory Password Rotation Gate"). **Notable finding:** the gate broke nine integration-test classes that had been acting as the seeded Site Admin without ever rotating — direct evidence the hole was real; their duplicated login helpers are now one shared rotation-aware `SiteAdminAuth`. Still open in Sprint 4: the broad code-review pass, the four remaining hardening-batch items (LIKE escaping, export/import memory bounds, client token-expiry), and profile portrait upload.
  - **Decisions recorded 2026-08-11 (user interview):** (1) portrait-upload image library = **SkiaSharp** (NuGet approved); (2) Idea Brainstorm chat stays **plain** for now — real LLM-backed chat deferred to *after* Sprint 4 (post-MVP; **now scheduled as Sprint 7** — see the next bullet); (3) ~~Site Admins get an idea-create path~~ **REVERSED later 2026-08-11:** Site Admins get **no** direct org-scoped create/edit paths and no org dropdowns — org content is created/edited via **View As act-as impersonation** (Sprint 6; D-MODE locked = full act-as, dual attribution); org + user administration stay direct as the bootstrap exception (spec: `20-feature-client-ui.md` → "Site Admin org-content mutation model"); (4) new page/flow UI is **comp-first** going forward. Full context in `SPEC/Bug Triage.md`.
  - **AI-assisted idea drafting — SCHEDULED AS SPRINT 7 (2026-08-11, user direction: "before the Azure deployment work but after the Postgres migration"). Azure deployment renumbered 7 → 8** (`sprint-08-azure-deployment.md`); the sprint count is now 8. Specs written ahead of the sprint: `SPEC/20-feature-ai-idea-assist.md`, `SPEC/30-Contracts.md` → "AI Idea Assist Contracts", `SPEC/40-test-strategy.md` → "AI Idea Assist", plan in `SPEC/sprints/sprint-07-ai-idea-assist.md`. **The `Anthropic` NuGet package is user-approved** (2026-08-11) for `Collega.Infrastructure`. Four design decisions locked the same day: scope gate = **Idea Types + an Org-Admin-editable scope statement**; similar-idea dedupe **deferred to v2** (v1 retrieves structured org data only — no embeddings, no `pgvector`, no Sprint 5 dependency); API key = **single platform-level key in server secrets** (per-org "Org AI credentials" stays backlogged); pre-fill = **Title, Description, Idea Type, Business Impact, Priority** as editable suggestions (UDFs + tags out of v1). Containment comes from structured output — the per-request JSON Schema's classification enums are built from the org's real option ids, and `next_question` is the model's only user-visible text field. Full context in `SPEC/Bug Triage.md`; canonical behavior in `SPEC/20-feature-ai-idea-assist.md`. Note the per-org `ai-key` contracts already in `30-Contracts.md` are deliberately **not** in Sprint 7 scope — v1 uses a single deployment-level key.
  - Note: the user landed their own `e2e/` Playwright browser suite on `dev` (commit `7a92dda`, 2026-08-11) and an AI-brainstorm WIP in `Ideas.razor`/`IdeaBrainstormModal.razor` (retained on `dev` by user choice) — both outside the sprint scope; leave them intact.
  - A migration bug was discovered and filed to `Bug Triage.md` TODO during Sprint 1 verification: the idea-type/business-impact migration fails its FK when applied over a DB that already has idea rows.
- **QA/Code Review has been skipped on nearly every merge so far**, by standing user direction, to move fast through the original MVP build-out. That's real accumulated debt — addressed in Sprint 4 of `95-next-sprints.md`, not before.
- Judgment calls — **all resolved 2026-08-11 (user interview; Sprint 4 P0)**: lockout **stays a fixed-window approximation** for MVP; JWT signing key **stays ephemeral for now — enforcing a stable `Auth:TokenSigningKey` is deferred to Sprint 8 (Azure; renumbered from 7 on 2026-08-11)**, already on that sprint's App Service config + risks + post-deploy checklist; `Status` name **stays `nvarchar(100)`** (25-char comp hint dropped, no migration); default `Status.Color`/`SortOrder` **confirmed final** (the 5 canonical `OrganizationDefaults` statuses). None required a code change now. Detail in `SPEC/sprints/sprint-04-qa-review-debt.md` → "Judgment Calls (resolved)".
- Test data note (updated 2026-08-10): the local `Collega` database on the `collega-sqlserver` Docker container (host port **1434**; the API's user-secrets connection string points here) was **dropped and re-seeded** during Sprint 1 live verification (user-approved), so the previously-noted manual records ("Quinn Tester" / "QA Test Org" / "QA Board") are gone — it now holds only the standard demo seed (2 orgs, 6 org users + 1 Site Admin, 4 boards, ideas). Note also there is an unrelated `sql-server-wwi` container occupying host port 1433 that can shadow LocalDB/1433 connection attempts.
- Last updated: 2026-08-11 (**Sprint 4 started** — the two security-first hardening items are fixed, spec'd, and covered by 19 new tests on `feature/sprint-04-security-hardening`; suite now 525 green, and the recorded baseline was corrected from a stale 500. Earlier same day: AI-assisted idea drafting scheduled as **Sprint 7**, pushing Azure deployment to **Sprint 8** — four design decisions locked by user interview, `Anthropic` package approved, and the full spec set written ahead of the sprint: `20-feature-ai-idea-assist.md`, the "AI Idea Assist Contracts" section of `30-Contracts.md`, the "AI Idea Assist" section of `40-test-strategy.md`, and `sprints/sprint-07-ai-idea-assist.md`. Earlier same day: Sprint 4 kicked off: the four open judgment calls — lockout / JWT key / status-name length / status defaults — all resolved via user interview, none needing a code change now; JWT-key enforcement routed to the Azure sprint, now Sprint 8. Earlier same day: Site Admin org-content mutation model decided: no direct Site Admin create/edit for org content, View As act-as impersonation instead — the idea-create path briefly assigned to Sprint 4 earlier today is removed as superseded; D-MODE locked = full act-as. Earlier same day: Sprint 3 completion + user-interview decisions — update this line whenever this section changes, not just when something is appended).

## Notes For Next Agent
- Read `SPEC/95-next-sprints.md` for current sprint scope, not the archive's original backlog.
- Use `Specs Overview.md` as the behavior entrypoint and `30-Contracts.md` for endpoint and payload authority.
- Keep this Current Status section short and current — if you're tempted to write more than a few sentences about something that happened, put the detail in `SPEC/implementation-agent-tracker-archive.md` and leave a one-line pointer here.
