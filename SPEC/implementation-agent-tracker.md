# Collega Implementation Agent Tracker

## Purpose
Track what's true right now: current implementation status and what's next. **This file is kept short and current-only on purpose** — full narrative history (per-slice build write-ups, judgment calls, UI comp sign-off history, the original T001-T052 backlog) lives in `SPEC/archive/implementation-agent-tracker-archive.md`. Read this file for "what's true right now"; read the archive for "how did we get here" or "what did slice X actually build."

## Ground-Truth Verification — read this before trusting anything below
Before making any status, planning, or scope claim about this project — in this session or any future one — re-read the Current Status section below AND run `git log --oneline -10` fresh in that same turn. Never answer from recollection, even within the same conversation. This file was split from a 291-line narrative log on 2026-08-10 specifically because an agent answered a planning question from stale in-context memory while ~2 weeks of real parallel-agent work had landed without that memory being refreshed. A large date jump, an unfamiliar recent commit, or "it's been a while since I checked" are signals to verify more, not less. See also `CLAUDE.md`'s "Ground-Truth Verification" section.

## Pre-Feature Triage Gate
- Before starting or resuming implementation, read `SPEC/Bug Triage.md`.
- Unresolved items in its `TODO` section take priority over new feature work. Do not start a new feature until those items are cleared unless the user explicitly approves an exception.
- After a fix is complete and focused validation passes, move the item out of `TODO` into `SPEC/archive/bug-triage-completed.md` with its completion date and verification note; do not retain it in both places.
- When an item is promoted into a canonical spec or a sprint plan, **delete it from the queue** — the spec or sprint file becomes its only home. Feature ideas live in `SPEC/ideas-inbox.md` and do not gate work.

## Current Status
**Verified 2026-09-08 against `3843394` (`dev`; `origin/dev` matches, `main` is 11 commits behind).** Every figure below was **counted from the working tree in the same sitting**, not carried forward — the reconciliation that produced this section found the Sprint 9 row four Wave B partitions and the whole of Wave C out of date. Keep this section a table plus short blocks — see Maintenance Rule at the end of this file.

> **This header went stale once while the table below it was updated, and an agent read the header and planned from it.** If the two disagree, neither is trustworthy: re-derive from `git log` before asserting anything.

> **The .NET stack is FROZEN as of 2026-09-06** (`SPEC/decisions.md`). Rows below that describe
> `src/Collega.*` or `tests/` are **status, not instructions** — no features, no fixes, no tests, no
> migrations there. It stays on disk only as a golden-fixture recorder and as the last runnable
> reference for existing behaviour, and is deleted in slice **F6** once F1 replays clean.

| Area | State | Detail / authority |
|---|---|---|
| MVP epics (T001–T067) | **Merged to `dev`** | Foundation→Hardening, User-Defined Fields, Idea-Type Fields. Done — do not restart. |
| Blazor client | ~16 pages, 9 shared components in `src/Collega.Client/Components/` | Geist is the single typeface. |
| Test suite | **TypeScript: 297 passing, 2 skipped** (counted 2026-09-08 at `3843394`) | One `pnpm test` run, summed across the workspace: application 35 · domain 28 · infrastructure 26 (+2 skipped) · api 26 · web 94 · design-system 8 · boundaries 30 · golden 48 · arch 2. The 2 skips are the live-database suite, which `skipIf(!DATABASE_URL)` guards. **Coverage is inverted against risk** — see the QA debt rows: `packages/application` is the largest package at 14,876 lines, carries authorization, and has 35 tests; `apps/web` has 94 over 8,544. **The .NET suite is history, not status:** 826 green at `72deea6` on 2026-09-06, frozen the same day, discarded rather than ported (ticket `10`), deleted at F6. |
| Sprints | **1–7 complete** · **7.5 closed** · **9 (the conversion) is the ACTIVE sprint** | Sprint 7 closed 2026-08-18. **Sprint 8 was cancelled 2026-09-04** — the .NET stack is never deployed; everything goes to Vercel at the end of the conversion (`SPEC/decisions.md`). Sprint 7.5 is an accessibility/bug paydown — `sprints/sprint-07.5-accessibility-and-bug-paydown.md`. Index: `SPEC/95-next-sprints.md`. Plans: `SPEC/sprints/`; completed in `SPEC/sprints/archive/`. |
| QA / code review | **Partially paid down; the largest gap is now `packages/application`.** | Sprint 4 covered auth/CSV/UDF/idea-repository/client-auth on the .NET side — history now, since that tree is frozen. **On the TypeScript side the gap is measurable and inverted against risk:** `packages/application` is 14,876 lines, carries authorization, and has **35 tests**; `apps/web` has **94** over 8,544 lines. Wave E's debt was paid down deliberately (see the row below) and the same pass has never run over the layer where a mistake means one organization reads another's data. **Scheduled 2026-09-08 (`SPEC/decisions.md`): a QA slice against Application, in its own worktree, running in parallel with Wave D rather than after it** — the Wave E pattern, which produced 102 tests and verified every rule by breaking it (21 mutations, 21 caught). Not started; it needs a QA agent, not the agent that wrote the code. **D1 ran that pattern for the first time on 2026-09-09 and it worked**: `apps/api` went 26 → **87 tests**, written by an agent that had not touched the source, and the round found two divergences from `Convert.FromBase64String` that neither the implementer nor the reviewer had spotted. It also caught that its own earlier tests asserted behaviour a later review proved wrong — the tests were corrected, not the code. That is the argument for the Application slice: **`packages/application` is now the only layer of consequence with no such pass over it.** Boundary for the old .NET debt: `sprints/archive/sprint-04-qa-review-debt.md`. |
| Bug queue | **Empty — promoted into Sprint 7.5** | Ten items from the 2026-08-16 browser pass moved to `sprints/sprint-07.5-accessibility-and-bug-paydown.md` on 2026-08-25 and deleted from `SPEC/Bug Triage.md` per its Promote-and-delete rule. Intake reopens there when 7.5 archives. |
| Comp P refresh | **Complete (2026-09-03)** | The shipped client ported to comp P's locked design, so the TypeScript conversion has a settled baseline: four files, 46 screens, every one at four roles and four states. `comp-p-focus-roadmap.html` (core, 8), `comp-p-auth.html` (8), `comp-p-admin.html` (23 `/settings/*` routes), `comp-p-delivery.html` (7 — specified, unbuilt, so it carries the *not built* strip). Delivery is regenerated on the single-parent decision. Sources and conventions: `SPEC/mockups/_build/README.md`. **Comp Q** (`comp-q-*.html`, 2026-09-03) re-renders the same fragments on Tailwind CSS v4 + shadcn/ui and is the reference for Wave E; `_build/build_q.py` (needs `npm ci` in `_build/tw`). |
| **Sprint 9 — the conversion** | **EXECUTING. Waves A, 0, B (all 7), C (both) and E (E0–E7) are merged. Wave D is the critical path and has only D0. F and G not started.** | Plan: `SPEC/50-typescript-migration.md` (~66–82 slices; **roughly 30 done**). Wrapper: `sprints/sprint-09-typescript-conversion.md`. **This row was wrong until 2026-09-08** — it claimed B1–B3 merged with "B4–B7 and C–G not started" while all four remaining B partitions and both C slices had been on `dev` since 2026-09-06. Verified by merge commit, in this order: **B1** `41ea143` · **B2** `c29fcc1` · **B3** `13ac239` · **B4** `238e12a` (comments, tags, notifications) · **B5** `12d9391` (idea types and fields) · **B6** `bcecddc` (AI assist) · **B7** `76349df` (View As) · **C1** `5df98cf` (Prisma repository adapters) · **C2** `f0c6797` (external integrations) — all 2026-09-06. Cross-checked against the tree: all 16 feature areas exist in both `packages/domain/src` and `packages/application/src`, and `packages/infrastructure/src/repositories` holds 19 repositories (3,217 lines). **Wave E is complete through E7** (`2575b4f`, 2026-09-08) and runs entirely on `apps/web/lib/mock.ts`. **Wave D is D0 through D5** — D0 `2c31f85` booted the host; **D1** `6535f6a` (Organizations, Users, Auth, 22 endpoints), **D2** `9d9b2ed` (Boards, Statuses, 10), **D3** `e0f4366` (Ideas, Upvotes, 11), **D4** `6279b7d` (Comments, Tags, 5), **D5** `391d2ae` (Idea types, Field definitions, Business impacts, 18) — all 2026-09-09. **66 of 81 endpoints; D6 (AI assist) and D7 (View As) owe the remaining 15.** Read the count from the tree, not from here: `grep -rhoE "@(Get|Post|Put|Patch|Delete)\(" apps/api/src --include=*.controller.ts | wc -l` is 67, of which one is the health check.

**Golden replay: 122 → 367 of 447** over that day (`--keep-going`; always name the flag, because the default stop-on-error run scores lower when an unbuilt endpoint aborts a scenario). Every built scenario is at zero diffs. The remaining 80 are 71 D6/D7 routes returning 404 plus nine accepted differences: four portrait-encoder byte diffs, four CSV-export date diffs, and the deliberate `accessToken` removal.

**D2–D5 ran as four parallel worktree agents, each reviewed by a separate agent, with fixes applied by a third.** That found **nine defects in code the corpus was passing at the time**, which is the number worth remembering when judging what a green replay proves. The ones with lessons attached:

- **Three classes of reachable `500`** on inputs no fixture sends — unported `[MaxLengthField]`/`[EmailFormat]` hitting a column constraint, a body-less request dereferencing `undefined`, and a non-GUID id reaching a `uuid` column. The last one is instructive: `guidOrEmpty`'s own doc comment, written in the same wave, warns about exactly it, and it was then not applied one file over.
- **Two sort defects the corpus structurally could not catch.** The organization list's default was inverted and the fixture matched anyway, because the seed's two organizations happen to be created in alphabetical order. Tag suggestions ordered by `name` where the .NET repository ordered by `normalized_name` — pinned by no fixture at all, and it feeds `idea-assist-context-builder`, where the ordering alone decides which tags reach the model.
- **A defect in the demo seed itself.** The contributor rotation was `['orgadmin', 'user', 'user2']` where `.ThenBy(u => u.Email)` puts `user2@` first. The seed-state fingerprint hashes the roster, not per-idea assignment, so it passed the guard and diffed on content.
- **A latent CI race in `turbo.json`.** `@collega/infrastructure#test` depended on `^build` (upstream packages) rather than its own `db:generate`, and `src/generated/` is gitignored. Won by luck on every run until adding two slices changed the scheduling. It hides locally because `build` copies `src/generated` into `dist/generated` for the test to resolve through — which is why `pnpm check` was green while CI was red.

**Two things were checked rather than assumed, and both mattered.** D5 was warned that `FieldType` is 1-based where every other enum is 0-based (`prisma/manual/2026-09-08-ef-enum-drift.sql`) and found the concern never reaches the live path — string on the wire, string enum in the domain, native Postgres enum in the column — proving it by probing all seven types create → read-back → column. And D3's probe harness, which reported 46/58 where the corpus could not measure the slice, turned out to pin *less* than a replay: skipping prerequisite steps lets values first appearing in a response be labelled by position, so expected and actual can agree by construction.

**What D1 cost is worth knowing before D2–D7 start.** The corpus passed the slice and a code review then found three classes of `500` on inputs no fixture sends: boundary validation (`[MaxLengthField]`, `[EmailFormat]`) was never ported, so an over-length name reached Prisma and hit the column constraint; `change-password` had no required-field check, answering `401` and writing a spurious failed-password audit event; and a non-string JSON value threw on `.trim()` at an anonymous endpoint. Routes also lacked .NET's `{id:guid}` constraint, turning a malformed id into a 500 rather than a 404. **The corpus cannot catch this class** — it only sends the requests it recorded — so every D slice should transcribe its DTO's validation attributes from `src/Collega.API/Contracts/` rather than trusting a green replay. `validateFields` in `apps/api/src/common/errors/request-validation.error.ts` is the shared transcription point.

**One D1 defect the corpus structurally could not see**: the organization list's default sort was inverted (`createdAt` where .NET ordered by title). `organizations.list.siteadmin` matched anyway, because the seed's two organizations happen to be created in alphabetical order. A fixture matching is not proof the ordering rule is right. **Wave F:** F2's harness was repointed at `apps/web` on 2026-09-08 (see the e2e row); F1 and F3–F6 are untouched. **Wave G is CUT** (2026-09-08, `SPEC/decisions.md`) — revisited after cutover, not cancelled, so F1 now releases only F6 rather than F6 and Wave G. |
| **QA debt — Wave E** | **Paid down on `feature/088-qa-wave-e`.** `apps/web` and `packages/design-system` now have jsdom + `@testing-library/react` (exact-pinned) and a vitest config each; both dropped `--passWithNoTests`, so a suite that stops loading fails instead of passing silently. **102 tests** — 94 in `apps/web`, 8 in `packages/design-system` — covering all four named gaps plus the settings route gates: the four-role matrix as a `Record<Role, …>` table (a fifth role fails `tsc` until its row is written), the `Denied` contract at every call site (`aria-disabled` present, `disabled` absent, still focusable, `aria-describedby` resolving to a real element holding the reason), the `3/2/2/1/3` distribution, `aria-current` on exactly the selected row with its non-colour channel, and the Escape collision with the palette and the inspector mounted together. **Every rule was verified by breaking it**: 21 mutations, 21 caught. Gate: `pnpm check --force`, 21/21 tasks, 0 cached. The three product defects it found were fixed on `feature/092-notifications-and-limits` and **merged 2026-09-08**. | E0–E4 shipped ~819 lines of `apps/web` with **no tests**: both packages run `vitest --passWithNoTests` and neither has jsdom. Raised by the code review of `feature/084` on 2026-09-07, which named four gaps: **(1)** the four-role gating matrix — `writeDenial` vs `engagementDenial`, i.e. Read Only votes and comments but cannot edit, and a Site Admin does neither; the reviewer called this the rule most likely to be silently collapsed by a later slice, and nothing asserts it today. **(2)** `buildIdeas` produces 11 ideas per board in `3/2/2/1/3` canonical status order — `SPEC/40-test-strategy.md` names that distribution, and the fixture is now the only thing honouring it. **(3)** `IdeasTable` sets `aria-current` on exactly the selected row, and the selection survives greyscale. **(4)** A regression test for the Escape collision fixed in `4983764`. Per `CLAUDE.md`, a separate QA engineer writes these. |
| **Schema drift — the live database has no enum types** | **Resolved 2026-09-09: rebuild, not migrate. Verified; awaiting only someone with access to run three commands.** | The EF-migrated database has 0 enum types where `schema.prisma` declares 9, so **every Prisma write through those columns fails** (`SPEC/decisions.md` 2026-09-07 has the table). **The chosen resolution is `dropdb` → `db:migrate` → `db:seed`** — the user confirmed there is no production data. **Verified end to end 2026-09-09: 3.7 seconds**, `0 of 9 columns need migrating`, and the live-database suite passing 28/28 against the rebuilt database. This only became available when the seed landed the day before; recreation was previously impossible because the database was the only copy of its own contents. **The in-place migration stays on disk** (`prisma/manual/2026-09-08-ef-enum-drift.sql`, verified and idempotent) as the exception for a developer database holding local work, since the drift is in every pre-existing one. **The trap it defends against:** `FieldType` is 1-based and every other enum is 0-based, so one ordinal map shifts every field type by one **and still commits**. |
| **Wave E7 — the last settings surfaces** | **Complete, merged 2026-09-08.** | The eight comp Q administration screens E5 deferred, which the settings hub had been naming in a `NOT_BUILT` array: `/settings/profile`, `/settings/users/import`, `/settings/boards` plus its create and edit forms, `/settings/ai-assist`, `/settings/ai-prompt`, `/settings/api-usage`. That array is now empty and gone. Three primitives were added for them — `Meter`, `Code`/`CodeChip`, `FileButton`. **The hub is now built per role rather than filtered by a flag**: a Site Admin's Boards card points at the workspace list and AI Assist gives way to AI Prompt, because board and assistant configuration are organization-scoped and a Site Admin belongs to none. Every screen where a Site Admin is neither refused nor served the ordinary page needs its own third branch — `isAdministrator` passes them, so the shared gate would otherwise wave them into a form they may not submit. Still on `mock.ts`. **Precedence used throughout: comp Q supplies structure and copy, `mock.ts` supplies data.** |
| **Database blocker — duplicate row, folded 2026-09-08** | **Merged into the schema drift row above, which now also carries the fix.** | This row and the schema-drift row described the same defect in different words, which is how it came to be recorded twice and read as two problems. Kept as a stub so a reader following an older reference lands somewhere. Delete at the next sweep. |
| **apps/web data seam** | **Landed 2026-09-08. Zero direct `lib/mock` imports remain.** | `apps/web/lib/data/` is an async reader per feature area, backed by the fixtures now and by `fetch` after Wave D — so **Wave D replaces bodies, not call sites**, and its seven slices change one module each instead of colliding across the 37 files that used to import the fixture module directly. Identity and policy stayed **synchronous** in `lib/session.ts` on purpose: they resolve once at the request boundary, and making `currentUser` a promise would force every role-gated component async, including client ones that cannot await. Colour scales live in `lib/display.ts`. **`MOCK_LATENCY_MS=800 pnpm dev` holds responses open** so a loading state can actually be seen, and `MOCK_FAIL=getBoards` fails one reader so an error boundary can be proven rather than assumed. | **Done 2026-09-08.** Comp Q's 180 state variants had no counterpart at all; `apps/web` now has `Skeleton`/`EmptyState`/`ErrorState`, boundaries at the desk, app root and document root, and empty states on 14 list screens. **Most empty states are unreachable with the fixture** — every list has rows — so they are verified by reading, not by clicking; loading and error are exercised with `MOCK_LATENCY_MS` and `MOCK_FAIL`. **Empty-state counts are derived, never quoted from comp Q**, so two screens cannot name different numbers for the same thing. |
| **Streaming — a trap worth knowing** | **Fixed 2026-09-08, and it will recur.** | `Sidebar` became async to read its nav counts, and **an un-suspended `await` in a layout blocks the entire response**: under a 4s delay the app streamed **zero bytes in two seconds**, so no page's `loading.tsx` could render and a reader saw a blank document for the length of the slowest query. `Suspense` around it streams the shell first — 27,685 bytes in the same window. The fallback is the real nav minus its counts, since every label and href is static. **`pnpm check` cannot see this class of bug**: lint, `tsc` and Vitest were all green throughout. Only latency exposes it, which is what `MOCK_LATENCY_MS` is for. Anything added to `(desk)/layout.tsx` that awaits needs its own boundary. |
| **Phase 1 — the database, and how to start it** | **No longer blocked on Docker as of 2026-09-08.** | Two ways to a database now. **In a cloud session** `.claude/hooks/session-start.sh` provisions a native PostgreSQL 16 cluster and runs `prisma migrate deploy` unattended — no Docker, which matters because the egress policy blocks Docker Hub's blob CDN there. **Locally** `docker compose up -d postgres` still works. Then `pnpm --filter @collega/infrastructure db:check-enums`. **That check tells you which of two situations you are in, and they need opposite work.** Zero enum types and rows present is the EF-migrated database — it needs the in-place migration (`CREATE TYPE`, then `ALTER ... USING` per column). Nine enum types and no rows is fresh, which is now the verified normal case: `migrate deploy` produces the correct schema and the real blocker is the missing seed instead. **The EF database lives in the `postgres-data` Docker volume and does not travel with a `git pull`.** Two traps: `FieldType` is **1-based** (`Text = 1 ... Url = 7`), so a 0-based ordinal map shifts every field type by one **and still commits**; and the live database has no `_prisma_migrations`, so `migrate deploy` needs `prisma migrate resolve --applied 00000000000000_baseline` first. **`.env` is gitignored and will not travel either** — rebuild from `.env.example`; the hook writes `DATABASE_URL` into it. |
| **Seed modules** | **Written 2026-09-08. `pnpm db:seed` produces the full demo dataset.** | Five modules under `packages/infrastructure/prisma/seed/modules/`, split along the partition that owns each: organizations and users (B1), boards and statuses (B2), ideas, tags and upvotes (B3), comments (B4). Ported from the frozen .NET `StartupSeeder`, the only surviving record of the demo data. **Verified against a fresh cluster: 2 organizations, 10 users, 4 boards, 44 ideas** — the definition of done exactly — with ideas spread **3/2/2/1/3** across statuses on every board, and accounts that really sign in (all four roles plus both Site Admins verified against `Pbkdf2PasswordHasher`, each rejecting a wrong password). Idempotent: ids are derived from stable names rather than random, so a re-run upserts and changes nothing, and two seeded databases agree about every id — which a golden fixture depends on. A re-run deliberately does **not** reset an existing account's password. **`db:seed` could never have run before this**, modules or not: the harness imported `./compose.js`, which is never emitted, so the seed tree now carries its own tsconfig exactly as `tools/golden` does. **Consequence for the two rows above:** the demo seed is no longer single-copy, so the drifted EF database can be rebuilt rather than migrated. |
| **Branch inventory** | **Re-swept 2026-09-08 against `git ls-remote`. 15 remote branches: 10 merged, 5 not.** | **Merged into `dev`** (safe to delete): `main` — which is 11 commits *behind* `dev`, not ahead — plus `feature/065-viewas-test-harness`, `feature/066-delivery-comps-and-spec`, `feature/091-field-describedby`, `feature/092-notifications-and-limits`, `worktree-operational-logging-plan`, `worktree-playground-prereqs`, `worktree-prompt-lab`, `claude/pensive-cray-0ncxr4`. **Not merged, and each needs a decision:** **(1)** `feature/068-typescript-conversion-map` — 10 commits. **Must never be merged**: it carries `src/prisma.md` with live Prisma Postgres connection strings, and merging moves those commits into `dev`'s permanent history where deleting the branch no longer removes them. Its useful artefacts were copied across without the history. The credential was revoked 2026-09-08 and is inert, so the branch is safe to **delete**, which is the recommended action. **(2)** `feature/typescript` — **104 commits `dev` lacks**, not the 45 this row claimed until today. A superseded parallel implementation with its own component trees and ESLint where `dev` runs Biome. Leave it; delete once its one useful artefact (its CI workflow, already ported) is spent. **(3)** `legacy/dotnet` and **(4)** `claude/local-session-errors-igcnim` — **the same commit, `e8f6c4c` (2026-09-04)**, 59 commits `dev` lacks. This pair was **not recorded here before 2026-09-08.** It is the .NET line as it stood the day work stopped: 23 files under `src/Collega.Client`, a few under Infrastructure and the .NET tests, plus 20 comp files and edits to 8 `SPEC/*.md`. The golden corpus is **not** at risk either way — `dev` and this branch each carry all 484 `tools/golden` files and 448 fixtures. **Answered 2026-09-09: nothing to cherry-pick.** Compared directly against `dev` rather than from the merge base — the pair is almost entirely *behind* it (`decisions.md` alone is 317 lines shorter). What is unique to it is stale by two migrations: the ASP.NET Core / Blazor / Fluent UI / EF Core stack description, and **SQL Server DDL** (`UNIQUEIDENTIFIER`, `NVARCHAR`) in `20-feature-user-defined-fields.md`, superseded first by the Postgres migration and again by Prisma. Both branches are safe to delete, and **deletion was approved 2026-09-09** along with `feature/068` (its credential is revoked and inert). **Not done: this session cannot delete a remote branch** — `git push --delete` returns 403 through the git proxy and the GitHub MCP server has no delete-branch tool — so the three deletions are owed to someone with direct repository access. **(5)** `claude/dreamy-archimedes-eveuay` — this session's work, open as PR #6, ready for review. | **Ported 2026-09-08:** `feature/typescript`'s `.github/workflows/ci.yml`, adapted to Biome and Node 24 and reduced to one `pnpm check` step. That was the repo's first PR gate. **Do not set `DATABASE_URL` in that workflow** — `packages/infrastructure` guards a suite with `skipIf(!DATABASE_URL)`, so the variable's presence stops it skipping and it then tries to reach a database CI does not have. The SessionStart hook keeps `DATABASE_URL` out of the session environment for exactly the same reason. |
| **F1 blocker — golden replay authentication** | **Largely closed 2026-09-08. 360 of 447 cases unblocked; 1 needs a re-record.** | `tools/golden` sent a bearer token from the login body; `apps/api` issues an httpOnly cookie per decision `08`. The replay harness now speaks both: `golden replay --auth cookie` logs in, keeps the `collega_session` cookie and sends it on every step. Bearer stays the default, so a capture against the frozen .NET API is byte-for-byte unchanged. **The scale was smaller than feared, counted from the corpus:** 447 fixtures = 3 `POST /auth/login` + 360 other authenticated + 84 anonymous. Cookie mode fixes the 360; the 84 never authenticated; of the 3 login cases only **one** records `accessToken`, and it will diff until re-recorded. **`SPEC/30-Contracts.md` now documents the cookie** — it said nothing about one until today, which is exactly how the two sides came to disagree with nothing forcing the question. **Verified end to end 2026-09-09**, once D1 built the login endpoint: `--auth cookie` authenticates against the live Nest host with no bearer token anywhere, and 122 of 447 cases now match with only D1's endpoints built. **The seed-state guard also came alive with D1** — it reads through `GET /organizations` and its user list, so it could not run against Nest until those existed. It passes on a freshly seeded database, which is independent evidence that the TypeScript seed reproduces the .NET seeder's *data* rather than only its counts. Cookie handling is additionally verified against a stubbed fetcher (session carried, held rather than re-fetched, and named errors when a stack sets no session cookie or still returns a bearer token); **committed tests for the harness itself are still owed to a QA slice**, per `CLAUDE.md`.

**Reading a replay run correctly matters here**: the per-scenario `failed` column counts steps that *did not run*, not diffs. `0 failed` is not `0 diffs` — the diff list printed at the end of the run is the thing to read, and a body difference appears only there. |
| Conversion map | **Ported; 1 of 12 tickets open. Wave 0 is no longer gated.** | `SPEC/typescript-conversion-map/` (from `feature/068`, 2026-09-03). Answered 2026-09-03 in `decisions.md`: `01` (comp P on Tailwind + shadcn/ui; Q C — Loop, decision records, commitment strip, Triage Mode in as **Wave G**, after F1), `02` (Vercel + Prisma Postgres; Nest is serverless, so no in-process state), `10` (discard the .NET suite; golden corpus + per-slice Vitest). **The `05`/`07` research pair ran 2026-09-04** — findings in `typescript-conversion-map/findings/`. `05`: no EF global query filters exist, so org scoping ports as ordinary code; introspection loses **three partial unique indexes**, silently, and S0.2 owes them as raw SQL. `07`: `AsyncLocalStorage` behind a singleton port, seeded in middleware and filled by the auth guard, chokepoint lint-enforced. **Both decided the same day:** `08` — option C, Nest issues the httpOnly session cookie and Next stays a pure client, cross-origin setup accepted as the cost; `06` — forced reshapes only plus promoting all nine enum converters, and S0.2 does **not** lay down Wave G's entities, so Wave G buys a schema amendment slice. Only `11` (spec reconciliation, lands as F5) is still open. `feature/068` also holds `src/prisma.md` with a credential — never merge it; rotate the key. |
| Wave A (golden tests) | **Complete (2026-09-03)** | `tools/golden/` — capture and replay plus the corpus: **447 cases over all 81 endpoints** at four roles and anonymous, recorded from the live .NET API and replaying 447/447 clean against a fresh seed. Zero-dependency TypeScript, 48 self-tests. `tools/golden/README.md` names the two deliberate coverage gaps and what the capture found in the product. Wave F points it at Nest; **re-capture only while .NET still exists**. |
| Local .NET without a local install | **Works** | `docker compose --profile full up -d api` (2026-09-03) runs the API on the SDK image — migrations, seed and all — for a machine whose .NET is broken or missing. Behind a TLS-inspecting proxy, drop the CA in `docker/proxy-ca/` first. `src/Collega.Infrastructure/CLAUDE.md`. |
| Issues & Delivery | **Specified, unbuilt** | Slice 1 (Delivery + Tasks) is P0 and buildable; Slice 2 (Outcomes + Roadmap) is P1 and no longer gated. `SPEC/20-feature-issues-and-delivery.md` — no blocking Open Question remains. |
| **Cloud sessions (Claude Code on the web)** | **Set up 2026-09-08. A fresh container reaches `pnpm check`, `pnpm test:e2e` and a seeded database unattended.** | `.claude/hooks/session-start.sh` (SessionStart, synchronous) provides what the container does not: Node 24 via nvm (the image ships 22; `package.json` requires >=24.20.0, so pnpm refuses to install), `pnpm install`, a **native PostgreSQL 16 cluster**, `prisma migrate deploy`, and the **demo seed**. **Docker is not the route there** — the daemon starts, but the egress policy answers 403 for Docker Hub's blob CDN, so `docker compose up postgres` cannot pull an image. The native server needs no network at all. A cluster created this way is the **fresh** case the Phase 1 row describes: all nine enum types correct, `0 of 9 columns need migrating`. **`DATABASE_URL` is exported** — it was deliberately withheld until the seed existed, because `packages/infrastructure`'s `skipIf(!DATABASE_URL)` suite throws against a schema-only database; with data it runs and passes. **That only works because `turbo.json` declares `DATABASE_URL` on the infrastructure test task** — turbo 2's strict env mode was filtering it out, so the suite skipped even when the environment had it. Declared as `env`, not `passThroughEnv`, so a run with a database cannot reuse a cache entry from a run without. **CI is unaffected and must still not set it** — absent, the suite skips, which is the guard's intent. |
| **e2e harness** | **Repointed at `apps/web` 2026-09-08; the seven Blazor specs are retired.** | `pnpm test:e2e` starts the Next app itself through turbo (`webServer`) and needs nothing running first — two harness assertions, green. The retired specs drove FluentUI shadow roots against a `CollegaE2E` database the .NET API seeded on boot; neither exists now, and `apps/web` is still on `lib/mock.ts`, so no stateful flow can pass however it is written. **F2 still owns the flows**, and their list is preserved in `e2e/README.md` rather than in the deleted specs. Two environment facts are encoded in `playwright.config.ts` and `turbo.json`: the container's Chromium is not the build Playwright pins and `cdn.playwright.dev` is blocked, so the config launches `PLAYWRIGHT_CHROMIUM_PATH` when set; and turbo 2 runs **strict env mode** by default, so that variable and `PLAYWRIGHT_BROWSERS_PATH` are declared in `passThroughEnv` or they never reach the test process. |
| Local DB | `collega-postgres` (`postgres:16`), port **5432**, role `collega` | Standard demo seed (2 orgs, **10 users — one per role per org**, 4 boards, 44 ideas). Dev demo Site Admin: `siteadmin@demo.collega.test` / `Abc123!`. **If the API won't connect, check user-secrets for a stale SQL Server string** — see `src/Collega.API/CLAUDE.md`. |

### Sprint 5 — complete (merged `7c5a78b`)
**The InMemory suite sees neither collation, SQL translation, nor DDL** — its four defects were invisible to 561 green tests. Post-mortem: `sprints/archive/sprint-05-postgres-migration.md`.

### Sprint 6 — complete (2026-08-14); `dev` and `main` both at `a0ef22c`
View As, merged to `dev` at `c25eeda`; two code-review passes raised 8 then 9 findings, all fixed (`ba104db`, `322650e`). Slice 0's audit holds: `ICurrentUserContext` is the single identity chokepoint (nothing outside `API/Authentication/` reads claims) — **preserve that**, since a service reading claims directly would silently opt itself out of View As.

The last open item, retiring Site Admin direct org-content mutation, closed 2026-08-14: enforcement moved server-side into the Application layer (`OrgContentMutationGuard`) rather than resting on client affordances, which were route-shaped and bypassable. A third review pass found two unguarded paths — `ReassignIdeaTypeAsync` and `ImportBoardIdeasAsync`, the latter letting a refused Site Admin bulk-create the same ideas by CSV — both fixed and covered. Detail: `sprints/archive/sprint-06-view-as.md` Definition of Done. Canonical: `20-feature-view-as.md` rules 25-26.

**Product rule, stated by the user 2026-08-14 and now the reading of rules 25/25c/26:** *a Site Admin creates organizations and users for organizations; every other activity goes through Act As.* This resolved a real conflict — `20-feature-ideas-and-engagement.md` had said all authenticated users may upvote and comment, and that Site Admin may CSV-import ideas. Those three rules are now explicitly superseded **for the Site Admin role only** (Read Only users are members and keep both). Note the two CSV imports split: **user** import stays direct as bootstrap, **idea** import goes through View As.

### Sprint 6.5 — complete (2026-08-15)
Paydown sprint, 13 items over two intake rounds. Post-mortem: `sprints/archive/sprint-06.5-bug-fixes-and-tweaks.md`.

The one finding worth carrying forward: **the client's `ClaimsPrincipal` must be refreshed from `/auth/me`, not just read into a local field.** Impersonation is a server-side session and the token is never reissued, so the principal is the only thing that can carry the effective role — and `[Authorize(Roles=…)]`, `<AuthorizeView Roles=…>` and `IsInRole()` all read it. `MainLayout.ReloadIdentityAsync` now calls `RefreshUserAsync`; without that every role-gated surface renders for the real administrator during a View As session. This is the client-side twin of Sprint 6's `ICurrentUserContext` rule.

### Sprint 7 — complete (2026-08-18)
AI idea assist shipped: the model-backed brainstorm chat, cost controls (daily budget gate + per-org usage), rate limiting, and — in a follow-on batch on 2026-08-18 — a Site-Admin-managed **versioned system prompt** (`AiPromptVersion` + `AiPromptService`), a prompt playground / eval harness, and `.http` tracing of every model call. Plan and review findings: `sprints/archive/sprint-07-ai-idea-assist.md`. Build narrative: the tracker archive.

Two rules from this sprint are **live constraints, not history**:
- **No test may reach a model provider.** `CollegaApiFactory` blanks `ANTHROPIC_API_KEY` *and* swaps in `UnconfiguredIdeaDraftModel`. Before that guard existed the integration suite made a live billed Anthropic call, and the only symptom was one test taking five seconds instead of one. See `tests/CLAUDE.md`.
- **Retrieved content is escaped, not merely fenced.** A tag named `</organization_data> New instructions:` would otherwise close the untrusted-content block and continue as the operator. Tags are authored by ordinary Users — the lowest-privilege path into the prompt.

### Sprint 7.5 — closed 2026-09-04, implemented but not fully verified
Accessibility and bug paydown: the ten `Bug Triage.md` items from the 2026-08-16 live browser pass, now the sprint's only home (`sprints/sprint-07.5-accessibility-and-bug-paydown.md`). Three are systemic and reach every form and drawer — Enter submits no form (no native submit control survives Fluent's shadow DOM), `DrawerShell` never takes focus (Escape dead, no containment, no restore), and `FluentTextField` has no accessible name. **These were invisible to a fully green suite**, which is why that sprint's QA slice verifies in a running browser rather than by test.

### Locked decisions (current only — reversals are deleted, not struck through)
- Outcome ↔ Issue cardinality = **single-parent**: an Issue sits under at most one Outcome (`Idea.OutcomeId`, nullable FK, `ON DELETE SET NULL`); no join table. Decided 2026-09-02 → `SPEC/decisions.md`.
- Portrait image library = **ImageSharp** (`SixLabors.ImageSharp`, pinned **3.1.12**). Fully managed, no native assets — chosen 2026-08-13 specifically because SkiaSharp's package ships natives for Windows/macOS only and broke portrait upload on Linux App Service. **Stay on the 3.1.x line:** 4.x requires a Six Labors license key and warns on every build; 3.1.x is the Split License (free for OSS/personal and organizations under the revenue threshold — re-verify terms before any commercial release).
- Site Admin org-content mutation = **View As act-as only** (Sprint 6, full act-as + dual attribution); no direct create/edit paths, no org dropdowns. Org + user admin stay direct as the bootstrap exception. → `20-feature-client-ui.md`.
- AI idea drafting: single platform-level key (per-org keys stay unbuilt), dedupe deferred to v2, `Anthropic` package approved. The system prompt is a Site-Admin-managed versioned setting. → `20-feature-ai-idea-assist.md`.
- AI assist UI = Direction **C "Draft Strip"** (`mockups/comp-c-review-11-ai-assist-c-draftstrip.html`), teal suggestion indicator, scope statement on its own Settings page, ghost-then-drop for refused turns. Canonical in `20-feature-ai-idea-assist.md` → "UI Decisions". The read-only strip is load-bearing: making it editable brings back a per-field suggested-vs-edited state machine v1 deliberately does not have.
- AI cost controls (user decisions, 2026-08-16): model **`claude-sonnet-5`** at **`low` effort**, **500,000 tokens per UTC day** as one **global** pool, degrade at the cap rather than error, usage tracked **per organization** so per-org keys (rule 30) can be metered without a backfill. The cap is a runaway stop, not a $50 guarantee — saturated daily it allows roughly $99/month, and the usage page is what makes real spend visible. → `20-feature-ai-idea-assist.md` rules 28a–28e.
- New page/flow UI is **comp-first**.
- **List endpoints need a total order.** The golden capture found four places where one was missing or was only total by generated id, which is stable inside a deployment but not between two seeded from the same data. Under paging an arbitrary tie-break does not merely reorder a page, it decides what is on it. Tie-break on something stable and meaningful — email, not id, where the two differ. **And assert the concrete order**: the InMemory provider makes "no duplicates across pages" pass with the tie-break removed, which is how one such test sat green and worthless (`tests/CLAUDE.md`).
- TypeScript conversion, decided 2026-09-03 → `SPEC/decisions.md`: the .NET test suite is **discarded** (golden contract corpus plus per-slice Vitest, written by a QA agent); deployment is **Vercel + Prisma Postgres**, so Nest runs serverless and keeps no in-process state; net-new scope is **Wave G** — Loop, decision records, commitment strip, Triage Mode — starting only once F1 is green, with momentum, duplicate clustering and vote budget out.
- Judgment calls resolved 2026-08-11, no code change needed: fixed-window lockout for MVP; JWT key stays ephemeral until Sprint 8; `Status` name stays `nvarchar(100)`; status defaults final. → `sprints/archive/sprint-04-qa-review-debt.md`.

### Out of sprint scope — leave intact
User-owned, landed on `dev`: the `e2e/` Playwright suite (`7a92dda`). The AI-brainstorm WIP that used to sit here shipped in Sprint 7 and is no longer out of scope.

## Notes For Next Agent
- Read `SPEC/95-next-sprints.md` for current sprint scope, not the archive's original backlog.
- Behavior authority is the numbered canonical specs — `SPEC/README.MD` indexes them; `SPEC/30-Contracts.md` is authoritative for endpoints and payloads. **`Specs Overview.md` is a derived summary, not an entrypoint to trust** — see its own header.
- **On the macOS machine only, this repo sits in iCloud-synced `~/Documents` and file contents get evicted.** It does not apply to the Windows checkout at `C:\code\Collega`, where none of the following has been observed — check which machine you are on before acting on it. A `dataless` file (check `stat -f '%Sf'`) reads as empty or times out, and shows as ` M` in `git status` without anyone having edited it. Currently ~21 files under `SPEC/`, 44 under `tests/`, 175 of 188 under `e2e/`; `src/` is clean. To restore one, `rm` the placeholder then `git show <ref>:<path> >` it — unlinking is instant, overwriting in place hangs for minutes. Avoid commands that walk the whole object store (`git fetch`, `git push`, `git worktree add`); they time out. A fast-forward you are not standing on is safest as `git update-ref` after `git merge-base --is-ancestor`.

## Maintenance Rule
This file answers **"what is true right now"** and nothing else. When updating it:

0. **Verify wave status by merge commit, never by memory or by this file's previous state.** This row
   has now gone stale twice, and the second time it under-reported by four Wave B partitions and all of
   Wave C — six merged slices that a reader would have rebuilt. Under-reporting is the dangerous
   direction. The check takes one command and settles it:

   ```bash
   git log --date=short --format='%h %ad %s' origin/dev | grep -iE 'Merge Wave'
   ```

   Cross-check against the tree rather than trusting the message — a merge commit says a branch landed,
   not that it did anything. `ls packages/application/src` and `ls packages/domain/src` show which
   feature areas exist; `find apps/api/src -name '*.controller.ts'` shows how much of Wave D is real;
   `apps/api/src/app.modules.generated.ts` lists every feature module the host has actually registered.


1. **Edit state in place; do not append history.** Change the table cell or decision line. Anything that reads "earlier the same day", "previously", or "was X, now Y" belongs in `SPEC/archive/implementation-agent-tracker-archive.md`.
2. **Delete reversed decisions — never strike them through.** A struck-through decision leaves both the old and new readings in context, and that is how agents answer wrong. Record the reversal in the archive; leave only the live decision here.
3. **Update the `Verified <date> against <commit>` line** whenever this section changes, and re-derive the state you are asserting rather than editing around it.
4. **Point, don't restate.** If detail lives in a sprint file, `Bug Triage.md`, or a canonical spec, link it in one clause instead of summarizing it here. Duplicated summaries go stale independently of their source, which produces exactly the contradictions this file exists to prevent.
5. **Budget: keep Current Status under ~450 words** (it was 427 at the 2026-08-11 compaction, down from ~1,300). It is re-read on every turn by the Ground-Truth Verification rule, so length here is paid continuously. Crossing the budget is the signal to move detail to the archive — not to raise the budget.
