# Sprint Archive

Completed sprint plans move here from `SPEC/sprints/`, unedited except for updating their `Status:` line to `Complete` with the completion date. Nothing in this directory represents active or planned work — check `SPEC/95-next-sprints.md` for the current sequence and `SPEC/sprints/` for active/upcoming sprint files.
