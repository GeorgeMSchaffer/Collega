{
  "name": "@collega/design-system",
  "version": "0.0.0",
  "private": true,
  "description": "Tokens and primitives from comp P. The only package apps/web may import.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
