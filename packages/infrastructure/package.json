{
  "name": "@collega/infrastructure",
  "version": "0.0.0",
  "private": true,
  "description": "Prisma client, repositories and external integrations. Implements application/domain abstractions.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@collega/application": "workspace:*",
    "@collega/domain": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
